import std::io
import w = os::windows
import os

enum e1
{
    A;
    B;
    C;
}

[distinct]
typealias stringgg = string;

main()
{
    io::printLn("%", os::getCommandlineArg());
    immut a := "moiz";
    aas := "moiz";

    ajsdj: stringgg = "sd";
    const bob: int = 90;

    bomb := (23022, 2);

    av: []i64 = []{1, 23, 23, 121233, 3 + bob};

    avv := cast (* [5] i64) av.data;

    for (i in @avv)
    {
        io::printLn("%", typeof(i));
    }
}