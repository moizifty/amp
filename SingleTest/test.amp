import std::io
import w = os::windows

enum e1
{
    A;
    B;
    C;
}

main()
{
    a := "sdsd";
    b: *string = *a;

    c: *void = cast (*void)b ? cast(*void) *b; 

    io::print("asda %", @cast(*string) c);
}