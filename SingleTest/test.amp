import std::io
import w = os::windows
import os

enum e1
{
    A;
    B;
    C;
}

main()
{
    io::printLn("%", os::getCommandlineArg());
    immut a := "moiz";
    aas := "moiz";

    bomb := (23022, 2);

    av: []int = []{1, 23, 23, 121233, 3};

    avv := cast (* [5] int) av.data;

    io::printLn("%", bomb);
}