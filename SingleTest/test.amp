import std::io
import w = os::windows

enum e1
{
    A;
    B;
    C;
}

main()
{
    const a := 10;

    b: [a]*int;

    for(num in b)
    {
        io::print("num %");
    }
}

sum<T>(arr: ..T) : T
{
    s: T;

    for(val in arr)
    {
        s = s + val;
    }

    return s;
}