import std::io
import w = os::windows
import os

enum e1
{
    A;
    B;
    C;
}

main()
{
    io::print("%", os::getCommandlineArg());
    a := "moiz";
    aas := "moiz";

    b := cast(*string) null;
}