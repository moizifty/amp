import std::io
import w = os::windows
import os

enum e1
{
    A;
    B;
    C;
}

main()
{
    io::print("%", os::getCommandlineArg());
    a := "moiz";
    aas := "moiz";

    b := cast(*string) null;

    p := cast(*int) (0x7c00);

    io::print("len: %", p);
}